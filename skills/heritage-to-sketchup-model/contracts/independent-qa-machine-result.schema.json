{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cad_to_sketchup.independent_qa_machine_result.2026-08-06",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "status", "qa_plan", "model_evidence", "derivations", "view_results", "automatic_checks", "unresolved"],
  "properties": {
    "schema": {"const": "cad_to_sketchup.independent_qa_machine_result.2026-08-06"},
    "status": {"enum": ["PASS", "FAIL"]},
    "qa_plan": {"type": "object", "required": ["path", "sha256"]},
    "model_evidence": {"type": "object", "required": ["path", "sha256"]},
    "derivations": {"type": "object", "additionalProperties": false, "required": ["cad", "model"], "properties": {"cad": {"type": "string", "minLength": 1}, "model": {"type": "string", "minLength": 1}}},
    "view_results": {"type": "array", "minItems": 1, "items": {"type": "object"}},
    "automatic_checks": {"type": "object"},
    "unresolved": {"type": "array"}
  }
}
